# CHANGELOG Generator

**Bounty**: $50 — [claude-builders-bounty #1](https://github.com/claude-builders-bounty/claude-builders-bounty/issues/1)

Auto-generates a structured `CHANGELOG.md` from git history. Commits are categorized into **Added**, **Fixed**, **Changed**, and **Removed** based on commit message prefixes.

## Setup

```bash
chmod +x changelog.sh
```

## Usage

```bash
# Since last git tag
bash changelog.sh

# Since specific ref
bash changelog.sh v1.0.0

# Between two refs
bash changelog.sh v1.0.0 v1.1.0
```

## Categorization Rules

| Prefix | Category |
|--------|----------|
| `feat:` / `add:` / `new:` / `implement:` / `introduce:` / `create:` | **Added** |
| `fix:` / `bug:` / `patch:` / `resolve:` / `hotfix:` / `repair:` / `correct:` | **Fixed** |
| `remove:` / `delete:` / `drop:` / `deprecate:` / `obsolete:` | **Removed** |
| Everything else (chore, docs, refactor, style, test, etc.) | **Changed** |

## Verified Output

Tested on this repository (no tags, 4 commits):

```markdown
# Changelog

## [2026-05-21] — All commits up to HEAD

### Added

- feat: CLAUDE.md template for Next.js + SQLite SaaS (`f9b7045`)
- feat: initial README with bounty board (`1aeae2a`)

### Changed

- Initial commit (`a80a580`)

### Fixed

- fix: add verification note, fix Drizzle syntax, add import examples (`388e128`)

---
Generated by [changelog.sh](.) on 2026-05-21
```

## Payment

- **BSC USDT**: `0x8ff2e1210434495c4f5629bd9d8bd4965a67b84c`

### Edge Cases Tested
- ✅ No tags → shows all commits
- ✅ Invalid ref → clean error message
- ✅ Merge commits → excluded
- ✅ Empty range → 0 commits (graceful)
- ✅ Mixed prefixes → correct categorization

## Payment

- **BSC USDT**: `0x8ff2e1210434495c4f5629bd9d8bd4965a67b84c`
