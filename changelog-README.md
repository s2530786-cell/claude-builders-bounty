# CHANGELOG Generator

**Bounty**: $50 — [claude-builders-bounty #1](https://github.com/claude-builders-bounty/claude-builders-bounty/issues/1)

Auto-generates a structured `CHANGELOG.md` from git history in 3 steps.

## Setup

```bash
chmod +x changelog.sh
```

## Usage

```bash
# Auto-detect last tag (recommended)
bash changelog.sh

# Since specific tag
bash changelog.sh v1.0.0

# Between two refs
bash changelog.sh v1.0.0 v2.0.0
```

## Categorization Rules

| Prefix | Category |
|--------|----------|
| `feat:` / `add:` / `new:` / `implement:` / `introduce:` / `create:` | **Added** |
| `fix:` / `bug:` / `patch:` / `resolve:` / `hotfix:` / `repair:` / `correct:` | **Fixed** |
| `remove:` / `delete:` / `drop:` / `deprecate:` / `obsolete:` | **Removed** |
| Everything else (chore, docs, refactor, style, test, etc.) | **Changed** |

## Verified Output

Tested on the [ION DEX](https://github.com/s2530786-cell/ION-DEX) repository (`v2026.05.20..HEAD`, 8 commits):

```
# Changelog

## [2026-05-21] — v2026.05.20 → HEAD

### Added
- feat(memory-bank): populate Cursor memory with wallet + audit requirements (`1a5f58b8`)
- feat(tools): add 22 new repos catalog for Cursor agent memory (`84d31dd0`)

### Changed
- docs(standards): add 12-factor-agent principles as Master's dead law (`7874eef6`)
- audit(contracts): Phase 2 Task 1 complete - static audit findings (`303745a6`)

### Fixed
- fix(merge): restore build helpers and align ApiSource after main merge (`f56daaa7`)
- fix(frontend): deduct protocol fee from swap minimum received (#8) (`da3980b6`)
- fix(deps): resolve 9 Dependabot alerts with npm overrides (`16b01bcd`)
- fix(swap): deduct protocol fee before slippage on min received (`763784c5`)

---
Generated by changelog.sh on 2026-05-21
```

## Payment

- **BSC USDT**: `0x8ff2e1210434495c4f5629bd9d8bd4965a67b84c`

## Edge Cases Handled
- No tags in repo → shows all commits
- Invalid ref → exits with clear error
- Empty range (same ref) → 0 commits, no crash
- Not a git repo → exits with message
- Merge commits → excluded automatically

## Payment

BSC (USDT): `0x8ff2e1210434495c4f5629bd9d8bd4965a67b84c`
